# DeskHabitat 项目计划书

> 文档状态：实施基线
> 目标平台：Windows 10/11（第一阶段）
> 技术栈：Electron + TypeScript + PixiJS 8 + Vite + electron-builder
> 产品定位：一个生活在桌面上的、可布置和养成的微型动物栖息地

## 1. 项目摘要

DeskHabitat 是一款运行在 Windows 桌面上的透明覆盖式 2D 应用。动物、地形、栅栏、食盆、水盆和装饰物由 PixiJS 渲染在透明、无边框的 Electron 窗口中。程序默认处于不打扰用户工作的“生活模式”，透明区域允许鼠标点击穿透；用户进入“布置模式”后，可以选择、拖动和放置设施。

项目的正式产品形态从一开始就按全屏透明桌面覆盖设计，但实现时同时保留固定尺寸的开发窗口。固定窗口只用于调试、自动化测试和排查渲染问题，不代表产品最终形态。

第一版要形成一条完整、可验证的玩法闭环：

1. 程序随桌面启动并显示透明栖息地。
2. 一只兔子在等距投影场景中自主活动。
3. 兔子拥有饥饿、口渴、休息等基础需求。
4. 兔子能够寻找食盆或水盆，并绕开栅栏。
5. 用户能进入布置模式，放置和移动基础设施。
6. 退出并重新启动后，动物与设施状态能够恢复。
7. 生活模式下不妨碍用户操作桌面和其他应用。

## 2. 产品目标

### 2.1 核心体验

- 动物像真实生活在桌面上一样持续活动，而不是只播放循环动画。
- 桌面生态在用户工作时保持安静、轻量、不抢焦点。
- 用户可以用很低的操作成本布置一个小型栖息地。
- 设施对动物行为产生可观察的影响。
- 程序重新启动后，栖息地仍保持连续性。

### 2.2 第一版成功标准

- Windows 10/11 上可安装、启动、退出和卸载。
- 透明窗口覆盖目标显示器，无系统标题栏和不透明背景。
- 生活模式下，透明区域的点击能传递给下方窗口。
- 布置模式下，用户能稳定选中、拖动、旋转或删除设施。
- 兔子连续运行 30 分钟无卡死、越界或穿过封闭栅栏。
- 在可到达食盆的情况下，兔子能够完成“饥饿→寻路→进食→恢复”闭环。
- 保存文件损坏时程序不会白屏或无法启动，而是回退到默认世界并保留诊断信息。
- 常规办公设备上空闲时 CPU 和 GPU 占用处于可接受范围，且能通过配置降低帧率。

### 2.3 第一版不做

- 不做联网账号、云存档、多人访问或社区分享。
- 不做商店、付费、成就、复杂经济系统。
- 不做完整地图编辑器或关卡编辑器。
- 不做 3D 渲染、物理引擎或导航网格。
- 不做 macOS/Linux 的正式适配，但代码中避免无必要的平台耦合。
- 不做 Windows 桌面内部 `WorkerW`/壁纸层注入。该方式依赖非稳定系统行为；第一版使用标准透明窗口。
- 不在第一版加入多种动物、繁殖、疾病和复杂社交关系。

## 3. 已确定的技术决策

| 项目 | 决策 |
| --- | --- |
| 桌面外壳 | Electron |
| 编程语言 | TypeScript，开启严格类型检查 |
| 2D 渲染 | PixiJS 8 |
| 构建工具 | Vite |
| 安装包 | electron-builder，Windows NSIS |
| 测试 | Node.js 内置测试运行器；关键桌面行为补充人工测试清单 |
| 包管理器 | npm |
| 场景类型 | 纯 2D 等距投影，不使用 Cocos 或其他可视化引擎 |
| 寻路 | 自研小型网格 A*，便于针对栅栏和设施规则定制 |
| 存档 | 版本化 JSON，主进程负责读写和迁移 |
| 首发平台 | Windows 10/11 x64 |
| 正式显示模式 | 全屏透明桌面覆盖 |
| 辅助显示模式 | 固定尺寸调试窗口 |

依赖版本在项目初始化时锁定到当时的稳定版本，并提交 `package-lock.json`。不要使用宽泛的 `latest` 作为可重复构建依据。

## 4. 用户模式与窗口行为

### 4.1 生活模式（默认）

生活模式用于日常运行：

- 窗口透明、无边框、不出现在任务栏。
- 默认不抢夺键盘焦点。
- 透明区域和非交互区域允许鼠标穿透。
- 动物继续移动、进食、喝水和休息。
- 单击动物可作为后续增强；第一版中可以只显示短暂状态气泡。
- 托盘图标始终提供进入布置模式、暂停、重置窗口和退出功能。

Electron 主进程通过 `setIgnoreMouseEvents(true, { forward: true })` 让鼠标事件传递给下层窗口，同时让渲染层继续获得鼠标移动信息。渲染层对动物或可交互对象进行命中检测，仅在需要交互的局部区域通知主进程临时关闭穿透。

必须对穿透切换进行节流并设置安全回退：

- 指针离开交互对象时立即恢复穿透。
- 窗口失去焦点或渲染进程异常时恢复穿透。
- 托盘提供“强制恢复鼠标穿透”选项。
- 开发环境提供快捷键恢复穿透，防止调试时锁住桌面。

### 4.2 布置模式

布置模式用于编辑栖息地：

- 暂时关闭窗口鼠标穿透。
- 显示轻量工具栏、网格、可放置区域和对象轮廓。
- 动物模拟可以继续低速运行，或按设置自动暂停；第一版默认暂停。
- 用户可以放置、选择、拖动、旋转和删除设施。
- 无效位置以红色轮廓显示，且不能确认放置。
- 保存完成或退出布置模式时隐藏所有编辑辅助元素。

第一版工具栏包含：

- 选择/移动工具。
- 栅栏。
- 食盆。
- 水盆。
- 一个装饰物。
- 删除。
- 保存并退出。
- 取消本次未确认操作。

### 4.3 暂停模式

- 停止模拟时钟和动画更新，保留当前画面。
- 恢复后不补算暂停期间的所有帧。
- 可选地按现实经过时间对饥饿和口渴做有限离线结算；第一版最多结算 8 小时。

### 4.4 调试窗口模式

通过开发命令或环境配置启用：

- 默认尺寸 900×600。
- 有可选背景色或棋盘格，用于检查透明边缘。
- 可打开开发者工具。
- 显示 FPS、实体数量、当前状态、路径节点和碰撞格。
- 使用与桌面模式相同的游戏和渲染代码，禁止维护两套业务实现。

## 5. 桌面覆盖与多显示器策略

### 5.1 第一阶段范围

第一版默认覆盖主显示器的工作区。设置中允许切换为：

- 主显示器工作区。
- 指定的单个显示器。
- 指定显示器的完整边界（包括任务栏所在区域，仅作为高级选项）。

不在第一版创建跨越所有显示器的单个超大窗口。多显示器坐标可能包含负值，并存在不同 DPI 缩放；更可靠的扩展方案是每个显示器创建一个独立覆盖窗口，共享同一模拟状态。

### 5.2 显示器变化

监听显示器新增、移除、分辨率变化和缩放变化。发生变化时：

1. 重新读取显示器边界。
2. 将覆盖窗口移动和缩放到目标区域。
3. 更新渲染器分辨率。
4. 将超出新可用区域的世界锚点安全地夹取回范围内。
5. 记录变化但不删除原世界数据。

### 5.3 置顶策略

第一版提供两种策略：

- `overlay`：显示在普通应用上方，适合典型桌宠体验。
- `desktop`：不强制置顶，减少对其他应用的覆盖。

默认采用 `overlay`，但必须允许用户从托盘快速切换。不要使用过高的系统窗口层级覆盖任务栏、系统安全界面或全屏游戏。

## 6. 安全与进程边界

Electron 必须遵循明确的进程隔离：

- 主进程负责窗口、托盘、显示器、文件系统和应用生命周期。
- preload 只暴露白名单 API。
- 渲染进程只负责界面、PixiJS、模拟和交互。
- `contextIsolation: true`。
- `nodeIntegration: false`。
- 启用 `sandbox`，除非有经过记录且无法替代的阻碍。
- 不允许渲染进程直接读取任意文件路径。
- IPC 通道使用常量和严格类型，禁止动态执行通道。
- 所有来自存档和 IPC 的数据都要做运行时校验。
- 生产环境只加载打包后的本地资源，不加载远程网页。

preload 暴露的最小 API：

```ts
interface DeskHabitatApi {
  window: {
    setPointerPassthrough(enabled: boolean): Promise<void>;
    setMode(mode: 'life' | 'build' | 'paused'): Promise<void>;
    getDisplayInfo(): Promise<DisplayInfo>;
  };
  save: {
    load(): Promise<SaveEnvelope>;
    write(snapshot: SaveSnapshot): Promise<SaveResult>;
  };
  settings: {
    load(): Promise<AppSettings>;
    update(patch: Partial<AppSettings>): Promise<AppSettings>;
  };
  app: {
    getVersion(): Promise<string>;
  };
  events: {
    onCommand(listener: (command: AppCommand) => void): () => void;
    onDisplayChanged(listener: (info: DisplayInfo) => void): () => void;
  };
}
```

## 7. 建议目录结构

```text
DeskHabitat/
├─ assets/
│  ├─ animals/
│  ├─ environment/
│  ├─ items/
│  ├─ ui/
│  └─ manifests/
├─ docs/
│  ├─ architecture.md
│  ├─ save-format.md
│  ├─ asset-guidelines.md
│  └─ manual-test-checklist.md
├─ src/
│  ├─ main/
│  │  ├─ app.ts
│  │  ├─ windows/
│  │  │  ├─ createOverlayWindow.ts
│  │  │  ├─ createDebugWindow.ts
│  │  │  └─ WindowModeController.ts
│  │  ├─ tray/
│  │  │  └─ createTray.ts
│  │  ├─ ipc/
│  │  │  ├─ registerIpcHandlers.ts
│  │  │  └─ channels.ts
│  │  ├─ display/
│  │  │  └─ DisplayManager.ts
│  │  └─ persistence/
│  │     ├─ SaveRepository.ts
│  │     ├─ SettingsRepository.ts
│  │     └─ migrations/
│  ├─ preload/
│  │  ├─ index.ts
│  │  └─ api.ts
│  ├─ renderer/
│  │  ├─ index.html
│  │  ├─ main.ts
│  │  ├─ app/
│  │  │  ├─ DeskHabitatApp.ts
│  │  │  └─ AppModeStore.ts
│  │  ├─ rendering/
│  │  │  ├─ PixiRenderer.ts
│  │  │  ├─ layers.ts
│  │  │  ├─ IsoTransform.ts
│  │  │  └─ DepthSorter.ts
│  │  ├─ world/
│  │  │  ├─ World.ts
│  │  │  ├─ WorldGrid.ts
│  │  │  ├─ WorldFactory.ts
│  │  │  └─ WorldSerializer.ts
│  │  ├─ simulation/
│  │  │  ├─ SimulationClock.ts
│  │  │  ├─ systems/
│  │  │  └─ components/
│  │  ├─ animals/
│  │  │  ├─ Animal.ts
│  │  │  ├─ Rabbit.ts
│  │  │  ├─ AnimalBrain.ts
│  │  │  └─ states/
│  │  ├─ navigation/
│  │  │  ├─ AStar.ts
│  │  │  ├─ NavigationGrid.ts
│  │  │  └─ PathFollower.ts
│  │  ├─ building/
│  │  │  ├─ BuildController.ts
│  │  │  ├─ PlacementValidator.ts
│  │  │  └─ catalog.ts
│  │  ├─ interaction/
│  │  │  ├─ HitTestManager.ts
│  │  │  ├─ PointerPassthroughController.ts
│  │  │  └─ SelectionController.ts
│  │  ├─ ui/
│  │  │  ├─ BuildToolbar.ts
│  │  │  ├─ AnimalStatusBubble.ts
│  │  │  └─ DebugOverlay.ts
│  │  └─ styles/
│  │     └─ global.css
│  └─ shared/
│     ├─ contracts/
│     ├─ constants.ts
│     ├─ ids.ts
│     └─ validation.ts
├─ tests/
│  ├─ unit/
│  ├─ integration/
│  └─ fixtures/
├─ build/
│  ├─ icon.ico
│  └─ installer/
├─ electron-builder.yml
├─ vite.config.ts
├─ tsconfig.json
├─ package.json
└─ README.md
```

不要为了匹配目录树而预先创建所有空文件。目录应随对应里程碑逐步建立。

## 8. 核心领域模型

### 8.1 实体标识

所有持久化实体使用稳定字符串 ID，例如 `rabbit_01`、`food_bowl_01`。ID 不依赖数组下标、渲染对象引用或当前坐标。

### 8.2 基础数据

```ts
type EntityId = string;

interface GridPosition {
  x: number;
  y: number;
}

interface WorldEntity {
  id: EntityId;
  kind: string;
  position: GridPosition;
  rotation: 0 | 1 | 2 | 3;
}

interface AnimalNeeds {
  hunger: number;   // 0..100，越高越需要进食
  thirst: number;   // 0..100
  energy: number;   // 0..100
  happiness: number;// 0..100
}
```

坐标使用浮点逻辑网格坐标；占用、寻路和建筑验证使用离散格坐标。渲染坐标永远由逻辑坐标计算，不反向把精灵位置当作权威状态。

### 8.3 设施

第一版设施类型：

- `fence`：阻挡通行，可按四方向连接。
- `food-bowl`：可储存食物，动物可以使用。
- `water-bowl`：可储存水，动物可以使用。
- `decoration`：只影响画面，第一版不参与需求计算。
- `rest-spot`：可延后至第一版后半阶段，用于恢复精力。

每个设施定义：

- 占用格。
- 可交互入口格。
- 渲染锚点。
- 允许旋转方向。
- 是否阻挡移动。
- 可使用次数或容量。
- 构建模式预览资源。

### 8.4 动物状态机

第一只动物为兔子。建议状态：

```text
Idle
  ├─> Wander
  ├─> SeekFood ─> Eat
  ├─> SeekWater ─> Drink
  └─> SeekRest ─> Rest

任意移动状态
  ├─> Repath
  └─> StuckRecovery
```

行为优先级：

1. 无法继续当前路径时重新寻路。
2. 口渴超过紧急阈值时找水。
3. 饥饿超过紧急阈值时找食物。
4. 精力过低时休息。
5. 非紧急需求按最高效用选择。
6. 无明确目标时待机或闲逛。

状态切换必须集中记录原因，便于调试。例如：

```ts
transitionTo('SeekFood', {
  reason: 'hunger_threshold',
  hunger: 82,
  targetId: 'food_bowl_01',
});
```

### 8.5 模拟时钟

- 使用固定时间步长更新核心模拟，建议 10 Hz。
- 渲染帧率可为 30 或 60 FPS，与模拟频率解耦。
- 每帧设置最大追帧次数，避免系统卡顿后出现“死亡螺旋”。
- 页面恢复可见时丢弃过大的单帧时间差。
- 离线结算单独计算，不伪造大量历史帧。

## 9. 等距投影与渲染

### 9.1 坐标转换

初始地块尺寸建议使用 128×64 像素：

```ts
screenX = originX + (gridX - gridY) * tileWidth / 2;
screenY = originY + (gridX + gridY) * tileHeight / 2;
```

反向转换用于布置模式鼠标选格：

```ts
gridX = ((screenX - originX) / (tileWidth / 2)
       + (screenY - originY) / (tileHeight / 2)) / 2;

gridY = ((screenY - originY) / (tileHeight / 2)
       - (screenX - originX) / (tileWidth / 2)) / 2;
```

转换函数必须有单元测试，覆盖原点、正坐标、小数坐标和往返误差。

### 9.2 渲染层

建议层级：

1. 地面阴影和地块。
2. 地面装饰。
3. 世界对象：动物、设施、栅栏。
4. 粒子和状态气泡。
5. 布置模式预览。
6. UI 和调试层。

世界对象容器开启排序，基础深度值由逻辑位置决定：

```ts
zIndex = Math.round((gridX + gridY) * DEPTH_SCALE) + depthOffset;
```

高物体使用脚底锚点参与排序，不使用精灵图片顶部。栅栏拐角和大型装饰需要明确 `depthOffset`，不能依靠插入顺序碰运气。

### 9.3 资源规范

- 原始图片保持透明 PNG 或 WebP。
- 正式动画使用 spritesheet JSON + texture atlas。
- 动物每个方向至少有 idle、walk、eat、drink 动画。
- 所有世界资源标记统一脚底锚点。
- 资源清单使用稳定别名，业务代码不散落硬编码路径。
- 第一阶段允许用程序绘制的菱形、色块和简易兔子占位，架构稳定后再替换美术资源。
- 避免未经许可的网络图片和临时素材进入正式包。

## 10. 导航、阻挡与卡死恢复

### 10.1 导航网格

- 世界维护独立的通行网格。
- 栅栏和阻挡设施修改网格版本号。
- 寻路请求记录所用网格版本。
- 设施变更使相关路径失效，动物在下一个模拟步重新规划。
- 动物本身第一版不互相阻挡，避免群体拥堵问题。

### 10.2 A* 规则

- 第一版使用四方向移动，确保栅栏含义清晰。
- 启发函数使用 Manhattan distance。
- 禁止穿过阻挡格。
- 目标设施提供一个或多个入口格；路径终点是入口格，不是设施占用格。
- 对不可到达目标返回显式失败结果，不返回部分路径冒充成功。
- 地图小于约 64×64 时无需过早优化。

### 10.3 卡死恢复

检测条件：

- 有路径但在指定秒数内位移不足。
- 连续多次重规划失败。
- 当前格变成阻挡格。
- 动物超出地图边界。

恢复顺序：

1. 清除当前路径并重新规划。
2. 尝试同类目标的其他入口格。
3. 尝试同类其他设施。
4. 返回最近可通行格。
5. 记录结构化诊断事件，禁止静默瞬移。

## 11. 建造系统

### 11.1 放置流程

```text
选择目录项
  → 创建半透明预览
  → 屏幕坐标转换为网格坐标
  → 计算旋转后的占用格
  → 校验边界、重叠和通行规则
  → 用户确认
  → 创建领域实体
  → 更新导航网格
  → 更新渲染对象
  → 标记存档为 dirty
```

### 11.2 校验规则

- 不允许超出可建造区域。
- 不允许设施占用格互相重叠。
- 食盆和水盆至少保留一个可通行入口格。
- 新增栅栏后不能把兔子所在格变成阻挡格。
- 第一版允许用户把动物围在封闭区域中，但动物必须能在区域内活动；无法访问需求设施时显示状态提示。
- 删除设施前取消所有以它为目标的动物行为。

### 11.3 撤销

第一版至少支持一次未确认操作取消。完整撤销/重做可以放到后续版本。如果第一版实现命令模式成本不高，可从以下接口开始：

```ts
interface BuildCommand {
  execute(world: World): void;
  undo(world: World): void;
}
```

## 12. 存档与设置

### 12.1 保存位置

使用 Electron 的用户数据目录，不把运行时存档写入安装目录或源码目录。

建议文件：

```text
userData/
├─ saves/
│  ├─ habitat.json
│  └─ habitat.backup.json
├─ settings.json
└─ logs/
```

### 12.2 存档信封

```ts
interface SaveEnvelope {
  schemaVersion: number;
  appVersion: string;
  savedAt: string;
  checksum?: string;
  data: SaveSnapshot;
}
```

`SaveSnapshot` 至少包含：

- 世界 ID 和随机种子。
- 地图尺寸与世界锚点。
- 兔子位置、需求、状态所需的最小数据。
- 所有设施及其容量。
- 游戏时间。
- 最后在线时间。

不保存 PixiJS 对象、纹理、函数、临时路径、选中状态或 UI 状态。

### 12.3 保存策略

- 关键建造操作完成后进行防抖自动保存。
- 正常退出前保存。
- 固定间隔保存 dirty 状态。
- 写入临时文件，校验完成后替换正式文件。
- 替换前保留最近一次可读备份。
- 加载顺序：正式存档 → 备份 → 默认世界。
- 每次迁移前保留原文件。

### 12.4 版本迁移

从第一份存档开始使用 `schemaVersion`。迁移函数必须逐版执行：

```ts
v1 -> v2 -> v3
```

禁止写一个无法验证的“任意旧版本直接转最新版”函数。

## 13. 托盘、设置与应用生命周期

托盘菜单第一版包含：

- 进入生活模式。
- 进入布置模式。
- 暂停/继续。
- 桌面层/置顶层切换。
- 显示器选择。
- 打开调试窗口（开发版）。
- 强制恢复鼠标穿透。
- 重置栖息地（需要二次确认）。
- 开机启动开关。
- 退出。

关闭行为：

- 不提供普通窗口关闭按钮。
- 托盘“退出”才真正结束应用。
- 系统关机或会话结束时尽力保存，但不能只依赖异步退出回调。
- 单实例运行；第二次启动时唤醒或聚焦现有实例的托盘状态。

## 14. 性能策略

第一版即建立以下约束：

- 生活模式默认 30 FPS；布置模式可升至 60 FPS。
- 模拟保持 10 Hz。
- 没有可见动画变化时允许降低渲染频率。
- spritesheet 统一批处理，避免大量独立纹理。
- 不在每帧创建临时数组、路径对象或大量闭包。
- 只在实体跨越排序边界或位置改变时更新深度。
- 寻路不在渲染循环内重复执行。
- 窗口尺寸或 DPI 改变时才重建相关渲染资源。
- 对象销毁时移除事件监听、ticker 回调和纹理引用。

性能验收应记录测试设备、分辨率、缩放、实体数量和帧率，不能只写“运行流畅”。

建议基准场景：

- 2560×1440、125% 缩放。
- 1 只兔子、50 段栅栏、10 个设施。
- 生活模式运行 30 分钟。
- 检查平均帧率、CPU、GPU、内存增长和路径请求数量。

## 15. 日志与诊断

日志级别：

- `info`：启动、模式切换、保存、加载、显示器变化。
- `warn`：路径失败、备份恢复、存档迁移、资源降级。
- `error`：渲染初始化失败、存档不可恢复、IPC 异常。
- `debug`：状态切换、命中区域、路径节点，仅开发环境开启。

日志不得记录用户文件内容、剪贴板或其他应用信息。生产日志应轮转并限制总大小。

开发调试面板显示：

- FPS 和模拟更新频率。
- 当前显示模式及鼠标穿透状态。
- 兔子状态、需求和目标。
- 当前路径与导航网格版本。
- dirty/save 状态。
- PixiJS 渲染器信息。

## 16. 测试策略

### 16.1 单元测试

必须覆盖：

- 等距坐标正反转换。
- 网格边界和占用。
- A* 可达、不可达、绕路和起终点相同。
- 设施旋转后的占用格。
- 放置校验。
- 动物需求增长和状态优先级。
- 状态机合法切换。
- 存档序列化、校验和逐版迁移。
- 离线时间上限。

### 16.2 集成测试

- preload API 与 IPC 通道契约。
- 主进程保存、备份和恢复流程。
- 建造后导航网格失效与重新寻路。
- 删除正在使用的设施后动物安全恢复。
- 模式切换时鼠标穿透状态一致。

### 16.3 人工桌面测试

Electron 透明窗口和鼠标穿透需要真实 Windows 环境验收：

- 100%、125%、150%、200% 缩放。
- 1920×1080 和 2560×1440。
- 单显示器和双显示器，含副屏位于主屏左侧的负坐标布局。
- 任务栏在底部、侧边以及自动隐藏。
- 睡眠唤醒。
- 锁屏解锁。
- 显示器热插拔。
- 系统“显示桌面”。
- 普通窗口、浏览器、文件管理器和全屏程序。
- 鼠标快速划过动物边缘时的穿透稳定性。
- 渲染进程崩溃后是否恢复穿透。

## 17. 分阶段实施计划

每个里程碑必须满足验收标准后再进入下一阶段。Codex 实现时应为每个阶段保持小而可审查的改动。

### M0：项目骨架与质量门槛

任务：

- 初始化 npm、TypeScript、Vite、Electron、PixiJS 和 Node.js 测试运行器。
- 配置主进程、preload、渲染进程三套构建入口。
- 配置 ESLint/格式化工具或等价的统一代码检查。
- 配置开发、测试、类型检查、构建、打包脚本。
- 设置 `contextIsolation`、禁用 `nodeIntegration`。
- 建立最小 IPC 类型契约。
- 更新 README 的开发命令和架构说明。

建议脚本：

```json
{
  "dev": "启动 Electron 开发环境",
  "typecheck": "运行 TypeScript 检查",
  "test": "运行 Node.js 测试",
  "lint": "运行静态检查",
  "build": "生成生产构建",
  "dist:win": "生成 Windows 安装包"
}
```

验收：

- `npm install` 后能启动空白透明 Electron 窗口。
- 类型检查、测试、构建命令全部通过。
- 渲染进程中无法直接访问 Node.js。
- 开发版能够正常关闭，不残留后台进程。

### M1：透明桌面窗口与托盘

任务：

- 创建覆盖主显示器工作区的透明无边框窗口。
- 实现开发固定窗口切换。
- 实现托盘图标和基础菜单。
- 实现生活、布置、暂停三种窗口模式。
- 实现鼠标穿透开关和强制恢复入口。
- 监听显示器变化并调整窗口。
- 实现单实例锁。

验收：

- 透明窗口不显示背景色和系统边框。
- 生活模式下能点击下方桌面图标和普通窗口。
- 布置模式下窗口可接收输入。
- 托盘可暂停、切换模式和退出。
- 改变分辨率后窗口恢复正确边界。

### M2：PixiJS 世界与等距地面

任务：

- 初始化 PixiJS 8 Application，背景 alpha 为 0。
- 建立渲染层和世界根容器。
- 实现等距坐标转换。
- 绘制可配置尺寸的菱形草地区域。
- 实现窗口缩放、DPI 和世界锚点更新。
- 添加调试网格、坐标和 FPS。

验收：

- 桌面上出现透明背景的等距草地。
- 调整窗口或缩放后画布和世界位置正确。
- 坐标转换单元测试通过。
- 生产模式不显示调试元素。

### M3：兔子渲染与基础模拟

任务：

- 建立模拟时钟。
- 建立动物模型和状态机。
- 使用占位资源实现 idle、walk 动画。
- 实现闲逛、等待和边界约束。
- 实现深度排序和朝向选择。
- 添加状态气泡和调试信息。

验收：

- 兔子能够在地图中持续随机活动。
- 兔子不会越出可通行边界。
- 动画方向与移动方向一致。
- 暂停后位置和需求停止变化。
- 30 分钟运行无明显内存持续增长。

### M4：导航和栅栏

任务：

- 实现导航网格和 A*。
- 实现路径跟随与重新寻路。
- 实现栅栏实体、占用和连接外观。
- 提供开发期放置栅栏的方法。
- 实现卡死检测与恢复。

验收：

- 兔子能够绕过简单 L 形障碍。
- 封闭区域外目标返回不可达。
- 动态增加栅栏后旧路径会失效。
- 兔子不会穿过栅栏。
- A* 主要边界测试通过。

### M5：需求与设施闭环

任务：

- 实现饥饿、口渴、精力。
- 实现食盆、水盆及入口格。
- 实现 `SeekFood/Eat/SeekWater/Drink`。
- 实现设施容量及缺货状态。
- 实现目标失效和替代目标选择。

验收：

- 饥饿达到阈值后兔子会寻找可达食盆。
- 到达入口格后播放进食状态并降低饥饿。
- 水盆流程同样成立。
- 目标不可达或被删除时不会卡死。
- 需求数值始终位于定义范围。

### M6：布置模式

任务：

- 实现工具栏和目录。
- 实现屏幕选格、放置预览和旋转。
- 实现选择、拖动和删除。
- 实现合法/非法位置提示。
- 设施变化后同步导航网格。
- 退出布置模式时恢复鼠标穿透。

验收：

- 用户可完成栅栏、食盆、水盆和装饰物的放置。
- 非法重叠与越界不能确认。
- 移动设施后兔子重新规划。
- 取消操作不污染世界状态。
- 离开布置模式后桌面操作恢复正常。

### M7：存档、设置与恢复

任务：

- 定义 v1 存档格式与运行时校验。
- 实现原子保存、备份和加载回退。
- 实现自动保存与退出保存。
- 保存动物、设施、时间和世界锚点。
- 实现设置文件、显示器选择、帧率和置顶策略。
- 实现有限离线结算。

验收：

- 重新启动后场景和兔子状态恢复。
- 人为破坏主存档后能从备份恢复。
- 主存档和备份都损坏时能启动默认世界。
- 保存中不包含 PixiJS 或临时 UI 数据。
- 设置和存档相互独立。

### M8：桌面体验加固

任务：

- 优化交互对象命中与穿透切换抖动。
- 完成多 DPI、多显示器和睡眠唤醒处理。
- 完成性能基准和资源清理。
- 加入错误边界、日志轮转和崩溃恢复。
- 完善托盘菜单和首次启动引导。
- 完成人工测试清单。

验收：

- 鼠标快速移动和点击时没有长期吞掉桌面输入。
- 渲染异常时能够恢复穿透或由托盘恢复。
- 睡眠和显示器变化后窗口尺寸正确。
- 基准场景连续运行达到性能目标。
- 所有高优先级人工测试通过。

### M9：打包与候选版本

任务：

- 配置稳定应用 ID；发布前由项目所有者确认发行者命名。
- 准备图标、版本信息和许可证。
- 配置 electron-builder Windows NSIS。
- 确认安装、升级、卸载和用户数据保留策略。
- 生成候选安装包。
- 在干净 Windows 用户环境进行冒烟测试。

验收：

- 安装包可在目标 Windows 版本安装和启动。
- 安装路径不需要管理员权限，除非发行策略明确要求。
- 卸载不误删用户明确要求保留的存档。
- 安装包内不包含源码地图、测试夹具或无关大文件。
- 候选版本的类型检查、测试、构建和人工清单全部通过。

## 18. Codex 实施规则

把本文件交给 Codex 后，应遵循以下工作方式：

1. 从 M0 开始，禁止跨里程碑一次实现全部功能。
2. 每次开始前检查仓库状态和现有改动。
3. 每次只实现一个清晰、可验证的纵向切片。
4. 修改前先阅读相关代码，不覆盖用户已有改动。
5. 每个新增核心算法同时添加测试。
6. 完成后运行与改动相称的类型检查、测试和构建。
7. 不为了“看起来完整”创建大量无实现的类和空目录。
8. 不加入计划外的大型框架、状态管理库或物理引擎。
9. 如官方 API 与计划中的伪代码不同，以当前官方 API 为准，并在文档中记录差异。
10. 遇到产品决策缺口时使用本计划第 20 节的默认值；只有会显著改变范围时才询问项目所有者。
11. 每个里程碑结束时更新 README、相关设计文档和人工测试清单。
12. 不把未授权的网络素材或密钥提交到仓库。

推荐给 Codex 的第一个实施指令：

> 阅读 `IMPLEMENTATION_PLAN.md`，只实施 M0“项目骨架与质量门槛”。先检查仓库现状，再建立最小可运行架构。完成后运行类型检查、测试和生产构建；不要提前实现 M1 或创建后续阶段的空壳类。最后报告改动、验证结果和 M1 开始前仍需确认的事项。

## 19. 发布前完成定义

一个功能只有同时满足以下条件才算完成：

- 行为符合本计划和当前阶段验收标准。
- 正常路径、边界条件和失败路径已考虑。
- TypeScript 严格检查通过。
- 相关自动化测试通过。
- 生产构建通过。
- 桌面相关功能已在真实 Windows 环境人工验证。
- 没有新增高优先级错误日志。
- 资源、事件监听和定时器能正确释放。
- 文档反映当前实际行为。
- 没有把下一里程碑的半成品混入当前交付。

## 20. 尚可调整的产品参数

以下参数不阻塞开发，未另行决定时使用默认值：

| 参数 | 默认值 |
| --- | --- |
| 初始地图 | 16×16 格 |
| 地块像素尺寸 | 128×64 |
| 正式生活模式帧率 | 30 FPS |
| 布置模式帧率 | 60 FPS |
| 模拟频率 | 10 Hz |
| 首只动物 | 兔子 |
| 初始设施 | 食盆、水盆、少量栅栏 |
| 初始显示器 | 主显示器 |
| 覆盖范围 | 主显示器工作区 |
| 窗口层级 | overlay，可从托盘切换 |
| 布置时模拟 | 暂停 |
| 自动保存防抖 | 最后一次变更后 2 秒 |
| 周期保存 | dirty 状态下每 60 秒 |
| 离线结算上限 | 8 小时 |
| 发布应用 ID | 暂用 `com.deskhabitat.app`，正式发布前确认 |

## 21. 主要风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 全屏透明窗口吞掉桌面输入 | 严重影响正常使用 | 默认穿透、局部命中切换、托盘强制恢复、异常安全回退 |
| 不同 DPI 导致坐标错位 | 交互与渲染不一致 | 统一逻辑坐标、集中缩放转换、多缩放人工测试 |
| 多显示器负坐标和热插拔 | 窗口丢失或世界越界 | DisplayManager、单屏优先、边界夹取、变化监听 |
| 动态栅栏使路径失效 | 动物卡死或穿墙 | 导航版本号、重新寻路、卡死恢复 |
| 存档格式快速变化 | 早期用户数据丢失 | 从 v1 开始版本化、逐版迁移、备份回退 |
| 全屏透明渲染耗电 | 办公体验下降 | 30 FPS、模拟解耦、空闲降频、基准测试 |
| PixiJS 对象侵入领域模型 | 难测试、难存档 | 模型与视图分离、渲染对象不进入快照 |
| 一次实现范围过大 | 难以定位问题 | 严格按 M0–M9 验收推进 |
| 桌面层行为跨系统不一致 | 平台适配成本扩大 | 第一版只承诺 Windows，封装平台能力 |

## 22. 后续版本方向

第一版稳定后再评估：

- 多只动物与动物间社交。
- 更多设施、装饰和主题地块。
- 天气、昼夜和季节。
- 多显示器各自独立栖息地。
- 完整撤销/重做。
- 资源包与模组清单。
- 成就和图鉴。
- 更精细的离线模拟。
- macOS 适配。
- 可选的桌面层集成实验，但不得替代可靠的标准窗口模式。

## 23. 官方参考

- Electron BrowserWindow：<https://www.electronjs.org/docs/latest/api/browser-window>
- Electron 安全建议：<https://www.electronjs.org/docs/latest/tutorial/security>
- Electron Screen API：<https://www.electronjs.org/docs/latest/api/screen>
- PixiJS 8 Application：<https://pixijs.com/8.x/guides/components/application>
- PixiJS 8 Assets：<https://pixijs.com/8.x/guides/components/assets>
- PixiJS Container 与排序：<https://pixijs.com/8.x/guides/components/scene-objects/container>
- electron-builder 配置：<https://www.electron.build/configuration>
